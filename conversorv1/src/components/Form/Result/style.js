import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems:"center",
        justifyContent:"center",
        marginTop:40},
    formLabel: {
        color: "#000000",
        fontSize: 18
    },
    formValor:{
        color:"#000000",
        fontSize:18,
        fontWeight:'bold'
    }
});